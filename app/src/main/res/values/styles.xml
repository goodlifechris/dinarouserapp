<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MyEditTextStyle" parent="@android:style/TextAppearance.Widget.EditText">
        <item name="android:background">@color/colortransparence</item> //NO UNDERBAR
        <item name="android:maxLines">1</item>
        <item name="android:height">28dp</item> //COMMON HEIGHT
    </style>
    <style name="customfontstyle" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/courbd</item>
    </style>

    <style name="customFontStyle" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/museo700_regular</item>
    </style>
    <style name="customFontStyleRobotoRegular" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>
    <style name="ratingBarColor" >
        <item name="android:color">@color/coloryellow</item>
    </style>
</resources>
